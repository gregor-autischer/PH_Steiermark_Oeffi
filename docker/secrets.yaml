# Secrets file for Home Assistant
# Add any secrets here that your configuration needs

# Example (not used by this integration as it's configured via UI):
# api_url: "https://verkehrsauskunft.verbundlinie.at/stt/XML_TRIP_REQUEST2"